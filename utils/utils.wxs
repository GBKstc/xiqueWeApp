
var utils = {
  /**
   * 截图小数点后两位
   * @returns number
   */
  toFix: function (obj) {
    var re = "/([0-9]+\.[0-9]{2})[0-9]*/";
    if(!obj){
      return "";
    }
    return obj.toString().replace(re, "$1");
  }
}

module.exports = {
  toFix: utils.toFix
}
